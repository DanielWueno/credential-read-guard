# Fixture para scripts/doctor.js: prueba el prefijo "redact-key:" contra un
# archivo real, con cwd apuntado aqui via runGuard(..., { cwd }). No
# representa una recomendacion de configuracion -- ver
# examples/.credentialguardignore.example para eso.
redact-key: Authority
